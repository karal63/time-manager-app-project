const ObservePlan = () => {
    return (
        <section className="bg-hero-pattern h-[calc(100vh-68px)] bg-contain relative pl-20">
            123
        </section>
    );
};

export default ObservePlan;
